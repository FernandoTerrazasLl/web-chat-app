<section class ="register-page">
    <app-form
        action="{{action}}" 
        method="{{method}}" 
        title="{{title}}" 
        submit-text="{{submitText}}" 
        alternative-btn-text="{{alternativeBtnText}}" 
        fields='{{fields}}'
    ></app-form>
</section>