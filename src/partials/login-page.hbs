<section class ="login-page">
    <form-component
        action="{{action}}" 
        method="{{method}}" 
        title="{{title}}" 
        submit-text="{{submitText}}" 
        alternative-btn-text="{{alternativeBtnText}}" 
        fields='{{fields}}'
    ></form-component>
</section>